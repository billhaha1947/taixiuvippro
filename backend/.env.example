# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://username:password@localhost:5432/dragon_fire_casino

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-use-random-string

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Game Configuration (Optional - can be managed via admin panel)
DEFAULT_WINRATE=45
